{
  "name": "nine-lives-corp-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vue-tsc -b && vite build && mkdir -p dist/server && cp worker/index.js dist/server/index.js", "check": "vue-tsc -b", "test": "node --test src/core/*.test.ts", "test:smoke": "node --test src/core/poc-flow.smoke.test.ts" },
  "dependencies": { "vue": "^3.5.0" },
  "devDependencies": { "@vitejs/plugin-vue": "^5.2.0", "typescript": "^5.7.0", "vite": "^6.0.0", "vue-tsc": "^2.2.0" }
}
